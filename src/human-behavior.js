const { goals } = require('mineflayer-pathfinder');
const FOODS=['bread','cooked_beef','cooked_porkchop','cooked_chicken','cooked_mutton','baked_potato','golden_carrot','apple','carrot','potato'];
const HOSTILE=new Set(['zombie','husk','drowned','skeleton','stray','spider','cave_spider','creeper','witch','pillager','vindicator','phantom','enderman']);
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
function nearestPlayer(bot,max=24){return bot.nearestEntity(e=>e&&e.type==='player'&&e!==bot.entity&&e.position&&bot.entity.position.distanceTo(e.position)<=max);}
function nearestHostile(bot,max=18){return bot.nearestEntity(e=>e&&e.type==='mob'&&HOSTILE.has(e.name)&&e.position&&bot.entity.position.distanceTo(e.position)<=max);}
async function eatIfHungry(bot){if(bot.food>14)return false;const food=bot.inventory.items().find(i=>FOODS.includes(i.name));if(!food)return false;try{await bot.equip(food,'hand');await bot.consume();console.log(`[${bot.username}] ate ${food.name}`);return true;}catch(e){console.warn(`[${bot.username}] eating failed: ${e.message}`);return false;}}
function flee(bot,e,d=10){const p=bot.entity?.position;if(!p||!e?.position)return;const dx=p.x-e.position.x,dz=p.z-e.position.z,len=Math.max(.1,Math.hypot(dx,dz));bot.pathfinder.setGoal(new goals.GoalNear(p.x+dx/len*d,p.y,p.z+dz/len*d,3));}
async function lookAround(bot){if(!bot.entity)return;try{await bot.look(bot.entity.yaw+(Math.random()-.5)*1.8,Math.max(-.7,Math.min(.7,bot.entity.pitch+(Math.random()-.5)*.5)),true);}catch(_) {}}
function startHumanBehavior(bot){if(bot.humanBehaviorStarted)return;bot.humanBehaviorStarted=true;let lastIdle=0,lastLook=0;const timer=setInterval(async()=>{if(!bot.entity||bot.autonomyBusy||bot.pathfinder.isMoving()||bot.pvp?.target)return;if(await eatIfHungry(bot))return;const danger=nearestHostile(bot);if(danger&&bot.entity.position.distanceTo(danger.position)<5){flee(bot,danger,12);return;}if((bot.health??20)<=7){bot.humanUrgency='critical';if(danger)flee(bot,danger,14);return;}const now=Date.now();if(now-lastLook>5000+Math.random()*7000&&Math.random()<.6){lastLook=now;await lookAround(bot);return;}if(now-lastIdle<9000)return;lastIdle=now;const r=Math.random();if(r<.22){await sleep(600+Math.random()*1600);return;}if(r<.42){await lookAround(bot);return;}const p=bot.entity.position,a=Math.random()*Math.PI*2,d=3+Math.random()*9;bot.pathfinder.setGoal(new goals.GoalNear(p.x+Math.cos(a)*d,p.y,p.z+Math.sin(a)*d,2));},900);bot.once('end',()=>clearInterval(timer));}
function registerHumanEvents(bot){bot.on('entityHurt',e=>{if(e===bot.entity){bot.lastDamageAt=Date.now();bot.humanUrgency=(bot.health??20)<=8?'critical':'alert';}});bot.on('death',()=>{bot.autonomyBusy=false;bot.autonomyStarted=false;bot.autonomyStage='respawned';bot.humanUrgency='respawn';});bot.on('playerCollect',(collector,item)=>{if(collector===bot.entity)console.log(`[${bot.username}] collected ${item?.itemStack?.name||'item'}`);});}
module.exports={startHumanBehavior,registerHumanEvents,nearestPlayer,nearestHostile,eatIfHungry};